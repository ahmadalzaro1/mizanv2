---
phase: 09.1-bias-auditor-rework
plan: 03
type: execute
wave: 2
depends_on: [09.1-01, 09.1-02]
files_modified:
  - mizan/frontend/src/pages/BiasAuditorPage.tsx
  - mizan/frontend/e2e/bias-auditor.spec.ts
autonomous: true
requirements: [BIAS-01, BIAS-02, BIAS-03]
must_haves:
  truths:
    - "BiasAuditorPage displays a 4-tab layout with Arabic labels: نظرة عامة, توزيع الثقة, مصادر البيانات, الإيجابيات الكاذبة"
    - "Run/re-run button and 4 overall metric cards are pinned above tabs and always visible"
    - "During SSE streaming audit, a progress bar with Arabic percentage updates in real time"
    - "Overview tab shows auto-generated Arabic insight summary (2-3 sentences) above the existing BiasChart"
    - "Confidence tab renders the ConfidenceHistogram 3x3 grid"
    - "Sources tab renders the SourceBreakdownTable"
    - "False positives tab renders the FalsePositiveList"
    - "Old cached results (without new keys) display gracefully without errors"
    - "E2E tests pass for the reworked page"
  artifacts:
    - path: "mizan/frontend/src/pages/BiasAuditorPage.tsx"
      provides: "Reworked Bias Auditor page with tabs, SSE progress, insight summary"
      exports: ["default (BiasAuditorPage)"]
    - path: "mizan/frontend/e2e/bias-auditor.spec.ts"
      provides: "Updated E2E tests for reworked Bias Auditor"
  key_links:
    - from: "mizan/frontend/src/pages/BiasAuditorPage.tsx"
      to: "mizan/frontend/src/lib/audit-api.ts"
      via: "import runAuditStream, getAuditResults, AuditRun types"
      pattern: "runAuditStream|getAuditResults"
    - from: "mizan/frontend/src/pages/BiasAuditorPage.tsx"
      to: "mizan/frontend/src/components/ConfidenceHistogram.tsx"
      via: "import and render in confidence tab"
      pattern: "ConfidenceHistogram"
    - from: "mizan/frontend/src/pages/BiasAuditorPage.tsx"
      to: "mizan/frontend/src/components/SourceBreakdownTable.tsx"
      via: "import and render in sources tab"
      pattern: "SourceBreakdownTable"
    - from: "mizan/frontend/src/pages/BiasAuditorPage.tsx"
      to: "mizan/frontend/src/components/FalsePositiveList.tsx"
      via: "import and render in false positives tab"
      pattern: "FalsePositiveList"
---

<objective>
Rework BiasAuditorPage.tsx with a 4-tab layout, SSE progress bar, and auto-generated Arabic insight summary. Wire in all three new components from Plan 02. Update E2E tests.

Purpose: This is the integration plan that transforms the Bias Auditor from a flat single-view page into Lina's research tool with multiple analysis dimensions. The tabbed layout, streaming progress, and insight summary are the three UX features that make the hackathon pitch compelling.

Output: Reworked BiasAuditorPage with tabbed interface, streaming audit, and insight summary. Updated E2E tests.
</objective>

<execution_context>
@/Users/ahmadalzaro/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ahmadalzaro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/09.1-bias-auditor-rework/09.1-CONTEXT.md
@.planning/phases/09.1-bias-auditor-rework/09.1-RESEARCH.md
@.planning/phases/09.1-bias-auditor-rework/09.1-01-SUMMARY.md
@.planning/phases/09.1-bias-auditor-rework/09.1-02-SUMMARY.md
@mizan/frontend/src/pages/BiasAuditorPage.tsx
@mizan/frontend/src/components/BiasChart.tsx
@mizan/frontend/src/components/ConfidenceHistogram.tsx
@mizan/frontend/src/components/SourceBreakdownTable.tsx
@mizan/frontend/src/components/FalsePositiveList.tsx
@mizan/frontend/src/lib/audit-api.ts
@mizan/frontend/src/lib/format.ts
@mizan/frontend/e2e/bias-auditor.spec.ts

<interfaces>
<!-- Key types and contracts from Plan 01 and Plan 02. -->

From mizan/frontend/src/lib/audit-api.ts (after Plan 02):
```typescript
export interface AuditResults {
  overall: OverallMetrics
  per_category: CategoryMetrics[]
  per_source?: SourceMetrics[]
  confidence_dist?: Record<string, ConfidenceDist>
  false_positives?: FalsePositiveSample[]
}
export interface AuditRun { id: string; computed_at: string; total_examples: number; duration_ms: number; results: AuditResults }
export type StreamCallback = (data: StreamProgress) => void
export async function runAuditStream(onProgress: StreamCallback): Promise<AuditRun>
export async function runAudit(): Promise<AuditRun>
export async function getAuditResults(): Promise<AuditRun>
export async function downloadAuditCsv(): Promise<Blob>
```

From mizan/frontend/src/components/ConfidenceHistogram.tsx (after Plan 02):
```typescript
interface ConfidenceHistogramProps {
  confidenceDist: Record<string, { scores: number[]; count: number }>
  categoryLabels: Record<string, string>
}
export default function ConfidenceHistogram(props: ConfidenceHistogramProps): JSX.Element
```

From mizan/frontend/src/components/SourceBreakdownTable.tsx (after Plan 02):
```typescript
interface SourceBreakdownTableProps { sources: SourceMetrics[] }
export default function SourceBreakdownTable(props: SourceBreakdownTableProps): JSX.Element
```

From mizan/frontend/src/components/FalsePositiveList.tsx (after Plan 02):
```typescript
interface FalsePositiveListProps { samples: FalsePositiveSample[] }
export default function FalsePositiveList(props: FalsePositiveListProps): JSX.Element
```

Existing CATEGORY_LABELS_AR (from backend, replicate for frontend):
```typescript
const CATEGORY_LABELS_AR: Record<string, string> = {
  race: 'عنصرية', religion: 'ديني', ideology: 'أيديولوجي',
  gender: 'جنساني', disability: 'إعاقة', social_class: 'طبقي',
  tribalism: 'عشائري', refugee_related: 'لاجئين',
  political_affiliation: 'سياسي', unknown: 'غير مصنف',
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rework BiasAuditorPage with tabs, SSE progress, and insight summary</name>
  <files>mizan/frontend/src/pages/BiasAuditorPage.tsx</files>
  <action>
Rewrite `BiasAuditorPage.tsx` to implement the 4-tab layout with pinned header, SSE streaming progress bar, and auto-generated Arabic insight summary. This replaces the existing flat layout.

**State management:**
```typescript
const [auditRun, setAuditRun] = useState<AuditRun | null>(null)
const [loading, setLoading] = useState(true)
const [running, setRunning] = useState(false)
const [progress, setProgress] = useState(0)
const [error, setError] = useState<string | null>(null)
const [activeTab, setActiveTab] = useState<TabId>('overview')
```

**Tab definitions:**
```typescript
const TABS = [
  { id: 'overview' as const, label: 'نظرة عامة' },
  { id: 'confidence' as const, label: 'توزيع الثقة' },
  { id: 'sources' as const, label: 'مصادر البيانات' },
  { id: 'falsepos' as const, label: 'الإيجابيات الكاذبة' },
]
type TabId = typeof TABS[number]['id']
```

**Category labels constant (for ConfidenceHistogram):**
```typescript
const CATEGORY_LABELS_AR: Record<string, string> = {
  race: 'عنصرية', religion: 'ديني', ideology: 'أيديولوجي',
  gender: 'جنساني', disability: 'إعاقة', social_class: 'طبقي',
  tribalism: 'عشائري', refugee_related: 'لاجئين',
  political_affiliation: 'سياسي', unknown: 'غير مصنف',
}
```

**Audit run handler (SSE streaming):**
Replace the existing `handleRunAudit()` with:
```typescript
async function handleRunAudit() {
  setRunning(true)
  setProgress(0)
  setError(null)
  try {
    const result = await runAuditStream((data) => {
      setProgress(data.progress)
    })
    setAuditRun(result)
  } catch (e) {
    setError('حدث خطأ أثناء تشغيل التدقيق. تأكد من تحميل النموذج.')
  } finally {
    setRunning(false)
    setProgress(0)
  }
}
```

**Auto-generated Arabic insight summary function:**
Create a `generateInsight()` helper that takes `AuditResults` and returns 2-3 Arabic sentences:
```typescript
function generateInsight(results: AuditResults): string {
  const { overall, per_category, per_source, false_positives } = results
  const sentences: string[] = []

  // 1. Overall F1 assessment
  const f1Pct = Math.round(overall.f1 * 100)
  if (f1Pct >= 80) {
    sentences.push(`يحقق النموذج أداءً جيداً بشكل عام بمعدل F1 يبلغ ${toArabicDigits(f1Pct)}٪.`)
  } else if (f1Pct >= 60) {
    sentences.push(`أداء النموذج متوسط بمعدل F1 يبلغ ${toArabicDigits(f1Pct)}٪، مع مجال للتحسين.`)
  } else {
    sentences.push(`أداء النموذج ضعيف بمعدل F1 يبلغ ${toArabicDigits(f1Pct)}٪ ويحتاج إلى تحسين كبير.`)
  }

  // 2. Weakest category
  const categoriesWithSamples = per_category.filter(c => c.sample_count > 0)
  if (categoriesWithSamples.length > 0) {
    const weakest = categoriesWithSamples.reduce((a, b) => a.f1 < b.f1 ? a : b)
    sentences.push(`أضعف فئة هي "${weakest.category_ar}" بمعدل F1 يبلغ ${toArabicDigits(Math.round(weakest.f1 * 100))}٪ فقط (${toArabicDigits(weakest.sample_count)} عينة).`)
  }

  // 3. False positive rate or hardest data source
  if (per_source && per_source.length > 0) {
    const hardest = per_source.reduce((a, b) => a.f1 < b.f1 ? a : b)
    sentences.push(`أصعب مصدر بيانات هو ${hardest.source} بمعدل F1 يبلغ ${toArabicDigits(Math.round(hardest.f1 * 100))}٪.`)
  } else if (false_positives && false_positives.length > 0) {
    sentences.push(`تم رصد ${toArabicDigits(false_positives.length)} حالة إيجابية كاذبة حيث صنّف النموذج محتوى غير مؤذٍ على أنه كراهية.`)
  }

  return sentences.join(' ')
}
```

**Page layout structure:**

```
<div dir="rtl" className="font-tajawal">
  {/* Header (unchanged from existing) */}
  <h2>...</h2>
  <p>...</p>

  {/* Run/re-run + CSV buttons (pinned above tabs) */}
  <div className="mb-4 flex items-center gap-4">
    {/* Same buttons as current, but handleRunAudit now uses SSE */}
  </div>

  {/* SSE Progress bar (visible only when running) */}
  {running && (
    <div className="mb-4">
      <div className="flex justify-between text-sm text-gray-500 mb-1 font-tajawal">
        <span>جارٍ التدقيق...</span>
        <span>{toArabicDigits(progress)}٪</span>
      </div>
      <div className="h-2 w-full rounded-full bg-gray-200">
        <div
          className="h-2 rounded-full bg-mizan-navy transition-all duration-300"
          style={{ width: `${progress}%` }}
        />
      </div>
    </div>
  )}

  {error && <div className="mb-4 ...">...</div>}

  {/* Overall metric cards (pinned above tabs, same as current) */}
  {results && overall && (
    <>
      <div className="mb-4 grid grid-cols-4 gap-4">...</div>

      {/* Tab navigation */}
      <div className="mb-6 flex gap-1 border-b border-gray-200">
        {TABS.map(tab => (
          <button
            key={tab.id}
            onClick={() => setActiveTab(tab.id)}
            className={`pb-2 px-4 font-tajawal text-sm font-medium border-b-2 transition-colors ${
              activeTab === tab.id
                ? 'border-mizan-navy text-mizan-navy'
                : 'border-transparent text-gray-500 hover:text-gray-700'
            }`}
          >
            {tab.label}
          </button>
        ))}
      </div>

      {/* Tab content */}
      <div>
        {activeTab === 'overview' && (
          <>
            {/* Auto-generated Arabic insight summary */}
            <div className="mb-6 rounded-lg bg-blue-50 border border-blue-200 p-4">
              <h4 className="text-sm font-bold text-blue-800 mb-2">ملخص التحليل</h4>
              <p className="text-sm text-blue-700 leading-relaxed">
                {generateInsight(results)}
              </p>
            </div>

            {/* Existing weakness alert (keep) */}
            {weakCategories.length > 0 && <div>...</div>}

            {/* Existing BiasChart (keep) */}
            <div className="rounded-xl bg-white p-6 shadow-sm">
              <h3>...</h3>
              <BiasChart data={perCategory} />
            </div>
          </>
        )}

        {activeTab === 'confidence' && (
          <div className="rounded-xl bg-white p-6 shadow-sm">
            <h3 className="mb-4 text-lg font-bold text-mizan-navy">
              توزيع درجات الثقة حسب الفئة
            </h3>
            <ConfidenceHistogram
              confidenceDist={results.confidence_dist ?? {}}
              categoryLabels={CATEGORY_LABELS_AR}
            />
          </div>
        )}

        {activeTab === 'sources' && (
          <div className="rounded-xl bg-white p-6 shadow-sm">
            <h3 className="mb-4 text-lg font-bold text-mizan-navy">
              أداء النموذج حسب مصدر البيانات
            </h3>
            <SourceBreakdownTable sources={results.per_source ?? []} />
          </div>
        )}

        {activeTab === 'falsepos' && (
          <div className="rounded-xl bg-white p-6 shadow-sm">
            <h3 className="mb-4 text-lg font-bold text-mizan-navy">
              عينات الإيجابيات الكاذبة
            </h3>
            <p className="mb-4 text-sm text-gray-500">
              أمثلة غير مؤذية صنّفها النموذج خطأً على أنها خطاب كراهية
            </p>
            <FalsePositiveList samples={results.false_positives ?? []} />
          </div>
        )}
      </div>

      {/* Audit metadata (keep at bottom) */}
      <div className="mt-4 text-center text-xs text-gray-400">...</div>
    </>
  )}

  {/* Empty state (same as current) */}
  {!results && !running && <div>...</div>}
</div>
```

**Imports to add:**
```typescript
import { runAuditStream } from '../lib/audit-api'
import ConfidenceHistogram from '../components/ConfidenceHistogram'
import SourceBreakdownTable from '../components/SourceBreakdownTable'
import FalsePositiveList from '../components/FalsePositiveList'
```

**IMPORTANT:**
- The 4 overall metric cards and run/CSV buttons are ABOVE the tabs — always visible regardless of active tab (per CONTEXT.md locked decision).
- All new data fields accessed with `??` fallback for backward compatibility with old cached results.
- Keep existing imports: BiasChart, toArabicDigits, getAuditResults, runAudit (keep runAudit as fallback), downloadAuditCsv.
- The insight summary uses template-based Arabic prose (per CONTEXT.md decision), NOT an LLM call.
- Tab style: underline (border-b-2) per discretion choice — clean and minimal.
  </action>
  <verify>
    <automated>cd "/Users/ahmadalzaro/Desktop/AI Hatespeech Project/mizan/frontend" && npx tsc --noEmit --project tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>
    - BiasAuditorPage has 4-tab layout with Arabic labels
    - Metric cards and buttons are pinned above tabs
    - SSE progress bar updates in real time during audit run
    - Overview tab has auto-generated Arabic insight summary
    - Each tab renders its corresponding component with fallback data
    - Old cached results render without errors (optional fields handled)
    - Zero TypeScript errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Update E2E tests for reworked Bias Auditor</name>
  <files>mizan/frontend/e2e/bias-auditor.spec.ts</files>
  <action>
Update the existing Bias Auditor E2E tests to account for the new tabbed layout. The tests should NOT trigger an audit run (MARBERT batch is 140s) — they verify the page structure and cached results display.

Replace the existing `bias-auditor.spec.ts` content with:

```typescript
import { test, expect } from '@playwright/test'

test.describe('Bias Auditor (Lina)', () => {

  test('page loads without error', async ({ page }) => {
    await page.goto('/audit')
    await page.waitForTimeout(2_000)
    expect(page.url()).toContain('/audit')

    // Verify page title is visible
    await expect(page.getByText('مدقق التحيز')).toBeVisible()
  })

  test('displays cached results with tabbed layout OR empty state', async ({ page }) => {
    await page.goto('/audit')

    const hasResults = await page.getByText('F1 الإجمالي').isVisible({ timeout: 10_000 }).catch(() => false)

    if (hasResults) {
      // Cached results path: verify metric cards
      await expect(page.getByText('الدقة')).toBeVisible()
      await expect(page.getByText('الاسترجاع')).toBeVisible()
      await expect(page.getByText('عدد الأمثلة')).toBeVisible()

      // Verify tab navigation exists with all 4 tabs
      await expect(page.getByRole('button', { name: 'نظرة عامة' })).toBeVisible()
      await expect(page.getByRole('button', { name: 'توزيع الثقة' })).toBeVisible()
      await expect(page.getByRole('button', { name: 'مصادر البيانات' })).toBeVisible()
      await expect(page.getByRole('button', { name: 'الإيجابيات الكاذبة' })).toBeVisible()

      // Overview tab should be active by default — check for insight summary or BiasChart
      const hasInsight = await page.getByText('ملخص التحليل').isVisible({ timeout: 5_000 }).catch(() => false)
      if (hasInsight) {
        await expect(page.getByText('ملخص التحليل')).toBeVisible()
      }

      // BiasChart SVG should be visible on overview tab
      const svg = page.locator('svg')
      await expect(svg.first()).toBeVisible({ timeout: 10_000 })

      // CSV download button visible
      await expect(page.getByText(/تحميل التقرير|CSV/)).toBeVisible()

      // Click confidence tab and verify it renders
      await page.getByRole('button', { name: 'توزيع الثقة' }).click()
      await expect(page.getByText('توزيع درجات الثقة حسب الفئة')).toBeVisible({ timeout: 5_000 })

      // Click sources tab
      await page.getByRole('button', { name: 'مصادر البيانات' }).click()
      await expect(page.getByText('أداء النموذج حسب مصدر البيانات')).toBeVisible({ timeout: 5_000 })

      // Click false positives tab
      await page.getByRole('button', { name: 'الإيجابيات الكاذبة' }).click()
      await expect(page.getByText('عينات الإيجابيات الكاذبة')).toBeVisible({ timeout: 5_000 })

    } else {
      // No cached results path: verify empty state
      await expect(
        page.getByRole('button', { name: /بدء التدقيق/ })
      ).toBeVisible({ timeout: 10_000 })
    }
  })

  test('does NOT trigger a new audit run', async ({ page }) => {
    await page.goto('/audit')
    await page.waitForTimeout(3_000)
    expect(page.url()).toContain('/audit')

    // Verify Arabic content is present (RTL rendering works)
    const htmlDir = await page.locator('html').getAttribute('dir')
    expect(htmlDir).toBe('rtl')
  })
})
```

**Key changes from existing tests:**
- Added tab verification (4 tab buttons with Arabic labels)
- Added tab click navigation to verify each tab renders
- Check for "ملخص التحليل" (insight summary) on overview tab
- Check for section headers when clicking through tabs
- Keep the "does NOT trigger a new audit run" test unchanged (prevents 140s MARBERT batch)
- All assertions use OR patterns where old cached results may lack new keys (tabs still render, just with empty data)
  </action>
  <verify>
    <automated>cd "/Users/ahmadalzaro/Desktop/AI Hatespeech Project/mizan/frontend" && npx tsc --noEmit --project tsconfig.e2e.json 2>&1 | head -20</automated>
  </verify>
  <done>
    - E2E tests verify 4-tab layout exists with Arabic labels
    - Tests click through all tabs and verify section headers
    - Tests handle both cached-results and empty-state paths
    - No test triggers the 140s MARBERT batch run
    - TypeScript E2E config compiles without errors
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes for both tsconfig.json and tsconfig.e2e.json
2. BiasAuditorPage renders with 4 tabs, metric cards pinned above
3. Overview tab shows Arabic insight summary + existing BiasChart
4. Other tabs render their components with data or empty fallbacks
5. SSE progress bar visible during streaming audit
6. Vite dev build succeeds without errors
7. E2E tests pass when run against the Docker Compose stack
</verification>

<success_criteria>
- BiasAuditorPage is a fully functional tabbed interface
- Real-time SSE progress replaces the silent 140s wait
- Auto-generated Arabic insight gives Lina a narrative for her research
- All three new analysis views are wired and rendering
- Backward compatibility with old cached results maintained
- E2E tests cover the reworked page structure
</success_criteria>

<output>
After completion, create `.planning/phases/09.1-bias-auditor-rework/09.1-03-SUMMARY.md`
</output>
