# Plan 8.2: Login + Dashboard + Layout Visual Polish

> **Wave 1** — Visual polish. Can run in parallel with Plan 8.1.

## Goal
The login, dashboard, and layout look polished and professional for the hackathon demo. Login migrated to Tailwind. Dashboard shows persona-themed cards. Layout widens for chart pages.

## Prerequisites
- All pages functional (Phases 1–7 complete)
- Tailwind CSS v3.4 configured with mizan-navy, mizan-surface, font-tajawal

---

## Tasks

### Task 1: Migrate Login page to Tailwind

**File:** `mizan/frontend/src/pages/Login.tsx`

Replace inline styles with Tailwind classes. Add subtle Mizan branding (tagline under logo).

```typescript
import { useState } from 'react'
import { useNavigate } from 'react-router-dom'
import { useAuth } from '../lib/auth'

export default function Login() {
  const { login } = useAuth()
  const navigate = useNavigate()
  const [email, setEmail] = useState('')
  const [password, setPassword] = useState('')
  const [error, setError] = useState('')
  const [loading, setLoading] = useState(false)

  const handleSubmit = async (e: React.FormEvent) => {
    e.preventDefault()
    setError('')
    setLoading(true)
    try {
      await login(email, password)
      navigate('/')
    } catch {
      setError('البريد الإلكتروني أو كلمة المرور غير صحيحة')
    } finally {
      setLoading(false)
    }
  }

  return (
    <div dir="rtl" className="flex min-h-screen items-center justify-center bg-mizan-surface font-tajawal">
      <div className="w-full max-w-sm rounded-xl bg-white p-8 shadow-lg">
        <h1 className="mb-1 text-center text-4xl font-bold text-mizan-navy">ميزان</h1>
        <p className="mb-6 text-center text-sm text-gray-500">
          منصة تدريب مشرفي المحتوى بالذكاء الاصطناعي
        </p>
        <form onSubmit={handleSubmit}>
          <div className="mb-4">
            <label className="mb-1.5 block text-sm font-medium text-gray-700">
              البريد الإلكتروني
            </label>
            <input
              type="text"
              value={email}
              onChange={(e) => setEmail(e.target.value)}
              required
              dir="ltr"
              className="w-full rounded-lg border border-gray-300 px-3 py-2.5 text-base focus:border-mizan-navy focus:outline-none focus:ring-1 focus:ring-mizan-navy"
            />
          </div>
          <div className="mb-5">
            <label className="mb-1.5 block text-sm font-medium text-gray-700">
              كلمة المرور
            </label>
            <input
              type="password"
              value={password}
              onChange={(e) => setPassword(e.target.value)}
              required
              dir="ltr"
              className="w-full rounded-lg border border-gray-300 px-3 py-2.5 text-base focus:border-mizan-navy focus:outline-none focus:ring-1 focus:ring-mizan-navy"
            />
          </div>
          {error && (
            <p className="mb-4 text-center text-sm text-red-600">{error}</p>
          )}
          <button
            type="submit"
            disabled={loading}
            className="w-full rounded-lg bg-mizan-navy px-4 py-3 text-base font-bold text-white transition-opacity hover:opacity-90 disabled:cursor-not-allowed disabled:opacity-60"
          >
            {loading ? 'جاري تسجيل الدخول...' : 'تسجيل الدخول'}
          </button>
        </form>
      </div>
    </div>
  )
}
```

### Task 2: Upgrade Dashboard with persona-themed cards

**File:** `mizan/frontend/src/pages/Dashboard.tsx`

Add color accents per persona and a subtitle explaining each section's purpose for the jury.

```typescript
import { Link } from 'react-router-dom'
import { useAuth } from '../lib/auth'

const cards = [
  {
    to: '/observatory',
    title: 'المرصد',
    persona: 'رانيا — مسؤولة سياسات',
    desc: 'تحليل اتجاهات خطاب الكراهية في الأردن على مدى ٨ سنوات',
    accent: 'border-red-500',
    bg: 'bg-red-50',
  },
  {
    to: '/audit',
    title: 'مدقق التحيز',
    persona: 'لينا — باحثة NLP',
    desc: 'تقييم عدالة نموذج MARBERT حسب فئات خطاب الكراهية',
    accent: 'border-blue-500',
    bg: 'bg-blue-50',
  },
  {
    to: '/train',
    title: 'التدريب',
    persona: 'خالد — مشرف محتوى',
    desc: 'تدريب المشرفين على تصنيف المحتوى العربي بمساعدة الذكاء الاصطناعي',
    accent: 'border-green-500',
    bg: 'bg-green-50',
  },
]

export default function Dashboard() {
  const { user } = useAuth()

  return (
    <div dir="rtl" className="font-tajawal">
      <h2 className="mb-1 text-2xl font-bold text-mizan-navy">
        مرحباً، {user?.full_name}
      </h2>
      <p className="mb-8 text-gray-500">
        اختر أحد الأقسام الثلاثة للبدء
      </p>

      <div className="grid grid-cols-3 gap-6">
        {cards.map(({ to, title, persona, desc, accent, bg }) => (
          <Link
            key={to}
            to={to}
            className={`block rounded-xl border-t-4 ${accent} ${bg} p-6 no-underline shadow-sm transition-shadow hover:shadow-md`}
          >
            <h3 className="mb-1 text-lg font-bold text-mizan-navy">{title}</h3>
            <p className="mb-3 text-xs font-medium text-gray-400">{persona}</p>
            <p className="m-0 text-sm leading-relaxed text-gray-600">{desc}</p>
          </Link>
        ))}
      </div>
    </div>
  )
}
```

### Task 3: Widen Layout max-width for chart pages

**File:** `mizan/frontend/src/components/Layout.tsx`

Change `max-w-[900px]` to `max-w-5xl` (1024px) to give D3 charts more room.

**Current (line 62):**
```html
<main className="max-w-[900px] mx-auto px-8 py-8">
```

**New:**
```html
<main className="max-w-5xl mx-auto px-8 py-8">
```

### Task 4: Migrate ProtectedRoute loading to Tailwind

**File:** `mizan/frontend/src/components/ProtectedRoute.tsx`

**Current (line 7):**
```typescript
if (isLoading) return <div style={{ fontFamily: 'Tajawal, sans-serif', padding: '2rem', textAlign: 'center' }}>جاري التحميل...</div>
```

**New:**
```typescript
if (isLoading) return <div className="p-8 text-center font-tajawal">جاري التحميل...</div>
```

---

## Files Changed (4 tasks, 4 files)

| File | Action | Description |
|------|--------|-------------|
| `mizan/frontend/src/pages/Login.tsx` | Modify | Tailwind classes replace inline styles |
| `mizan/frontend/src/pages/Dashboard.tsx` | Modify | Persona-themed cards with color accents |
| `mizan/frontend/src/components/Layout.tsx` | Modify | max-w-5xl (1024px) for chart room |
| `mizan/frontend/src/components/ProtectedRoute.tsx` | Modify | Tailwind loading state |

## Verification

1. **Login looks polished:** Centered card with shadow, Mizan branding, RTL inputs
2. **Dashboard cards:** 3 cards with distinct color accents (red/blue/green) and persona names
3. **Charts have room:** Observatory and Bias Auditor charts render without horizontal clipping
4. **No inline styles remain:** Login and ProtectedRoute use Tailwind exclusively
5. **Build succeeds:** `npm run build` with zero TS errors

---

*Created: 2026-03-02*
